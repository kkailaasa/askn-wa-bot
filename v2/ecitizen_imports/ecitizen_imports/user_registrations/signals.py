#def register_signals():
    #tracking.anymail_tracking_opened.connect(track_email_opened, dispatch_uid='track_email_opened')

# Call the register_signals() function to register the signal handlers
#register_signals()
